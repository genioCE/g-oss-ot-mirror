# Example OPC UA → MQTT (edit endpoints, nodes)
[[inputs.opcua]]
  name = "opcua1"
  endpoint = "opc.tcp://plc.local:4840"
  security_policy = "None"
  nodes = [
    {name="tag1", namespace="2", identifier_type="s", identifier="Sensor1/Value"}
  ]

[[outputs.mqtt]]
  servers = ["tcp://mosquitto:1883"]
  topic = "ot/opcua"
